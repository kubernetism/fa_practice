# Desktop ↔ Web Compatibility Fix Plan

> **Desktop**: ~265 IPC handlers | **Web**: ~130 server actions | **Coverage**: ~50%
>
> This checklist tracks every gap between the Electron desktop app and the Next.js web app.
> Mark items `[x]` as they are completed.

---

## Phase 1: Critical — Core POS Functionality

### 1.1 Purchase Receiving Workflow
- [x] Add `receivedQuantity` column to `purchaseItems` schema *(already existed)*
- [x] Create `receivePurchase` action — receive goods, update inventory, create cost layers
- [x] Create `cancelPurchase` action
- [x] Create `getPendingPurchases` action
- [x] Update `updatePurchaseStatus` to handle partial receiving
- [ ] Add purchase receiving UI on `/purchases` page

### 1.2 Sales Tabs (Open Orders)
- [x] Create `src/actions/sales-tabs.ts` with all actions:
  - [x] `getSalesTabs` — list open/on_hold/closed tabs
  - [x] `getSalesTabById` — single tab with items
  - [x] `createSalesTab` — new tab
  - [x] `updateSalesTab` — update tab metadata
  - [x] `deleteSalesTab` — delete tab
  - [x] `addTabItem` — add product/service to tab
  - [x] `updateTabItem` — update item quantity/price
  - [x] `removeTabItem` — remove item from tab
  - [x] `closeTab` — close tab *(checkout via getTabItemsForCheckout + createSale)*
  - [x] `holdTab` — put tab on hold
- [ ] Create `/pos-tabs` page with tab list
- [ ] Create `/pos-tabs/[tabId]` page for tab detail
- [ ] Create `/pos-tabs/[tabId]/checkout` page

### 1.3 Inventory Counts (Physical Stock Verification)
- [x] Create `src/actions/inventory-counts.ts` with all actions:
  - [x] `getInventoryCounts` — list count sessions
  - [x] `createInventoryCount` — start count (full/cycle/spot/annual)
  - [x] `getInventoryCountById` — single count with items
  - [x] `addCountItem` — record counted item
  - [x] `updateCountItem` — update counted quantity
  - [x] `removeCountItem` — remove item from count
  - [x] `finalizeCount` — complete count and auto-create adjustments
  - [x] `getVarianceReport` — variance analysis
  - [ ] `getReconciliationSummary` — reconciliation stats
- [ ] Create `/inventory-counts` page (or integrate into `/inventory`)

### 1.4 Stock Transfers (Create & Receive)
- [x] Add missing columns to `stockTransfers` schema: `transferNumber`, `userId`, `serialNumbers`, `receivedBy` *(already existed)*
- [x] Create transfer actions in `src/actions/inventory.ts`:
  - [x] `createStockTransfer` — initiate inter-branch transfer
  - [x] `receiveStockTransfer` — complete transfer, update both branch inventories
  - [x] `getPendingTransfers` — list pending/in-transit transfers
  - [x] `cancelStockTransfer` — cancel pending transfer
- [ ] Add stock transfer UI on `/inventory` page

### 1.5 Receipt Generation
- [x] Create `src/actions/receipts.ts`:
  - [x] `generateReceipt` — generate structured receipt data for a sale
  - [x] `getReceiptSettings` — get receipt format configuration
  - [ ] `generatePaymentHistoryReceipt` — payment history receipt
- [ ] Add receipt preview/download to sales detail and POS checkout
- [ ] Support receipt customization from business settings

### 1.6 Auto GL Posting (Journal Entry Automation)
- [x] Create `src/lib/accounting/gl-posting.ts` utility:
  - [x] `postSaleToGL` — Debit Cash/AR, Credit Revenue + Tax
  - [x] `postReturnToGL` — Reverse sale GL entries
  - [x] `postPurchaseToGL` — Debit Inventory, Credit AP/Cash
  - [x] `postExpenseToGL` — Debit Expense, Credit Cash/AP
  - [x] `postStockAdjustmentToGL` — Debit Variance, Credit Inventory
  - [x] `postReceivablePaymentToGL` — Debit Cash, Credit AR
  - [x] `postPayablePaymentToGL` — Debit AP, Credit Cash
- [x] Add missing columns to `journalEntries` schema: `referenceType`, `referenceId`, `isAutoGenerated`, `reversalEntryId` *(already existed)*
- [x] Integrate GL posting into `createSale` action
- [x] Integrate GL posting into `createReturn` action
- [x] Integrate GL posting into `receivePurchase` action
- [x] Integrate GL posting into `createExpense` action
- [x] Integrate GL posting into stock adjustment actions
- [x] Integrate GL posting into receivable/payable payment actions

### 1.7 FIFO Cost Layer Consumption
- [x] Create `src/lib/accounting/cost-layers.ts` utility:
  - [x] `consumeCostLayers` — consume oldest layers on sale for COGS
  - [x] `restoreCostLayers` — restore layers on return
  - [x] `createCostLayer` — create layer on purchase receive
  - [x] `getInventoryValuation` — calculate current FIFO inventory value
- [x] Integrate cost layer consumption into `createSale` action
- [x] Integrate cost layer restoration into `createReturn` action
- [x] Integrate cost layer creation into purchase receiving

### 1.8 Categories CRUD Management
- [x] Create `src/actions/categories.ts`:
  - [x] `getCategories` — list all categories
  - [x] `getCategoryTree` — hierarchical tree structure
  - [x] `getCategoryById` — single category
  - [x] `createCategory` — create category
  - [x] `updateCategory` — update category
  - [x] `deleteCategory` — delete category (check for products first)
- [ ] Create `/categories` page with tree view and CRUD

### 1.9 Sales — Missing Actions
- [x] Add `updatePaymentStatus` action to `sales.ts`
- [x] Add `getSalesRangeSummary` action (date range summary)
- [ ] Add `fixPaymentStatus` utility action
- [x] Auto-create AR record when payment method is "receivable"/"credit"

---

## Phase 2: Important — Financial & Accounting Features

### 2.1 AR/AP Aging Reports & Extended Actions
- [x] Add to `receivables.ts`:
  - [x] `getCustomerBalance` — customer credit balance
  - [x] `getOverdueReceivables` — overdue list
  - [x] `getReceivableAgingReport` — 30/60/90/120+ day aging
  - [x] `getReceivableById` — single receivable detail
  - [x] `getReceivablesByCustomer` — filter by customer
  - [x] `cancelReceivable` — cancel with reason
  - [x] `getReceivablePayments` — payment history
  - [x] `syncReceivablesWithSales` — reconciliation
- [x] Add to `payables.ts`:
  - [x] `getSupplierBalance` — supplier balance
  - [x] `getOverduePayables` — overdue list
  - [x] `getPayableAgingReport` — 30/60/90/120+ day aging
  - [x] `getPayableById` — single payable detail
  - [x] `getPayablesBySupplier` — filter by supplier
  - [x] `cancelPayable` — cancel with reason
  - [x] `getPayablePayments` — payment history
- [ ] Add aging report UI to `/receivables` and `/payables` pages

### 2.2 Financial Reports (Missing)
- [x] Add to `reports.ts`:
  - [x] `getTrialBalance` — trial balance report
  - [x] `getCashFlowStatement` — cash flow report
  - [x] `getInventoryValuationReport` — FIFO inventory value report
  - [x] `getDailySalesReport` — daily detail
  - [x] `getInventoryReport` — stock analysis
  - [x] `getRevenueReport` — revenue analysis
  - [x] `getExpensesReport` — expense analysis
  - [x] `getReceivablesReport` — AR analysis
  - [x] `getPayablesReport` — AP analysis
  - [x] `getPurchaseReport` — purchase analysis
  - [x] `getReturnsSummaryReport` — returns analysis
  - [x] `getCommissionReport` — commission analysis
  - [ ] `exportReport` — PDF/Excel export
- [ ] Update `/reports` page with all report types

### 2.3 Tax Collections
- [x] Create `src/actions/tax-collections.ts`:
  - [x] `getTaxSummary` — tax collected summary
  - [x] `getTaxByPeriod` — tax by date range
  - [x] `getTaxSaleDetails` — tax breakdown per sale
  - [x] `getTaxReport` — full tax report
  - [ ] `exportTaxReport` — export
- [ ] Create `/tax-collections` page

### 2.4 Discount Management
- [x] Create `src/actions/discount-management.ts`:
  - [x] `getDiscountSummary` — discount usage summary
  - [x] `getDiscountDetails` — discount breakdown per sale
  - [x] `getDiscountReport` — full discount report
  - [x] `analyzeDiscountImpact` — impact on revenue
- [ ] Create `/discount-management` page

### 2.5 Expenses — Missing Actions
- [x] Add `getExpenseById` to `expenses.ts`
- [x] Add `updateExpense` to `expenses.ts`
- [x] Add `getExpensesByCategory` to `expenses.ts`
- [ ] Update `/expenses` page with edit functionality

### 2.6 Commissions — Missing Actions
- [x] Add `createCommission` to `commissions.ts`
- [x] Add `getCommissionById` to `commissions.ts`
- [x] Add `deleteCommission` to `commissions.ts`
- [x] Add `getAvailableInvoices` to `commissions.ts`
- [ ] Add auto-commission calculation on sale with referral
- [ ] Update `/commissions` page with create/delete

### 2.7 Chart of Accounts — Missing Actions
- [x] Add `getAccountById` to `chart-of-accounts.ts`
- [x] Add `getAccountBalance` to `chart-of-accounts.ts`
- [x] Add `getBalanceSheetFromCOA` (from COA, not reports)
- [x] Add `getTrialBalanceFromCOA` (from COA, not reports)

### 2.8 Journal Entries — Missing Actions
- [x] Add `getJournalEntryById` to `journal-entries.ts`
- [x] Add `updateJournalEntry` to `journal-entries.ts`
- [x] Add `getJournalEntriesByDateRange` to `journal-entries.ts`
- [x] Add `getGLTransactions` to `journal-entries.ts`

### 2.9 Account Balances (Period Snapshots)
- [x] Create `src/actions/account-balances.ts`:
  - [x] `calculatePeriodBalances` — compute daily/monthly/yearly snapshots
  - [x] `getBalancesByPeriod` — retrieve period balances
  - [x] `getAccountBalanceHistory` — historical balance trends
- [ ] Integrate period balance calculation into GL posting

---

## Phase 3: Moderate — Management & Administrative

### 3.1 Vouchers — Missing Actions
- [ ] Add `getVoucherById` to `vouchers.ts`
- [ ] Add `updateVoucher` to `vouchers.ts`
- [ ] Add `generateVoucherCode` to `vouchers.ts`
- [ ] Add `validateVoucher` to `vouchers.ts`
- [ ] Add `redeemVoucher` — use voucher in sale
- [ ] Integrate voucher redemption into POS/createSale

### 3.2 Users — Missing Actions
- [ ] Add `changePassword` action (current password + new password)
- [ ] Add `updatePermissions` action
- [ ] Add `checkPermission` utility
- [ ] Add password change UI to `/users` or `/settings`

### 3.3 Returns — Missing Actions
- [ ] Add `approveReturn` action (post to GL)
- [ ] Add `rejectReturn` action
- [ ] Add approval workflow UI to `/returns` page

### 3.4 Stock Adjustments — Missing Actions
- [ ] Add `reverseStockAdjustment` action
- [ ] Add reverse UI to `/inventory` page

### 3.5 Products — Missing Actions
- [ ] Add `importProducts` — bulk CSV/Excel import
- [ ] Add import UI to `/products` page

### 3.6 Customers — Missing Actions
- [ ] Add `checkCustomerLicense` — verify license status/expiry
- [ ] Add `getExpiringLicenses` — customers with expiring licenses
- [ ] Add license compliance alerts to dashboard

### 3.7 Referral Persons — Missing Actions
- [ ] Add `getReferralPersonById` to `referral-persons.ts`
- [ ] Add `getReferralPersonsForSelect` — dropdown data
- [ ] Add `updateReferralCommission` — update commission amount
- [ ] Add `getCommissionHistory` — history per referral person

### 3.8 Services — Missing Actions
- [ ] Add `getActiveServices` to `services.ts`
- [ ] Add `getServiceById` to `services.ts`
- [ ] Add `getServiceByCode` to `services.ts`
- [ ] Add `searchServices` to `services.ts`
- [ ] Add `deleteServiceCategory` to `services.ts`
- [ ] Add `getActiveServiceCategories` to `services.ts`
- [ ] Create `src/actions/sale-services.ts` — selling services in POS

### 3.9 Branches — Missing Actions
- [ ] Add `getActiveBranches` to `branches.ts`

### 3.10 Messages — Missing Actions
- [ ] Add `markAllAsRead` to `messages.ts`
- [ ] Add `deleteMessage` to `messages.ts`

### 3.11 Settings — Missing Actions
- [ ] Add `getSettingsByCategory` to `settings.ts`
- [ ] Add `updateBulkSettings` to `settings.ts`
- [ ] Add `syncBranchSettings` — sync across branches
- [ ] Add `exportSettings` — export to JSON
- [ ] Add `importSettings` — import from JSON
- [ ] Add `getBusinessSettingsByBranch` — branch-level overrides

### 3.12 Audit Logs — Missing Actions
- [ ] Add `getAuditLogsByUser` to `audit-logs.ts`
- [ ] Add `getAuditLogsByEntity` to `audit-logs.ts`
- [ ] Add `getAuditLogsByDateRange` to `audit-logs.ts`
- [ ] Add `exportAuditLogs` — export

### 3.13 Todos — Missing Actions
- [ ] Add `getTodoById` to `todos.ts`
- [ ] Add `updateTodo` (full update, not just status)
- [ ] Add `getTodoCounts` — counts by status
- [ ] Add `getAssignableUsers` — users available for assignment

---

## Phase 4: Security & Data Quality

### 4.1 Input Sanitization
- [ ] Create `src/lib/validation/sanitize.ts`:
  - [ ] `sanitizeName` — clean name fields
  - [ ] `sanitizeEmail` — clean email
  - [ ] `sanitizePhone` — clean phone
  - [ ] `sanitizeText` — generic text cleaning
  - [ ] `sanitizeForStorage` — XSS prevention
- [ ] Create `src/lib/validation/validate.ts`:
  - [ ] `isValidEmail` — email format check
  - [ ] `isValidPhone` — phone format check
- [ ] Apply sanitization to all create/update server actions

### 4.2 Customer Data Encryption
- [ ] Create `src/lib/security/encryption.ts`:
  - [ ] `encryptCustomerData` — encrypt government IDs, license numbers
  - [ ] `decryptCustomerData` — decrypt for display
- [ ] Apply encryption to customer create/update actions
- [ ] Apply decryption to customer read actions

### 4.3 Audit Logging Integration
- [ ] Create `src/lib/audit/logger.ts`:
  - [ ] `createAuditLog` — log action with old/new values
- [ ] Integrate audit logging into all create/update/delete actions
- [ ] Log login/logout events
- [ ] Log void/refund/adjustment events

---

## Phase 5: UI Pages — Missing Screens

### 5.1 Missing Pages
- [ ] `/pos-tabs` — Sales tabs management (Phase 1.2)
- [ ] `/tax-collections` — Tax collections (Phase 2.3)
- [ ] `/categories` — Categories management (Phase 1.8)
- [ ] `/discount-management` — Discount analytics (Phase 2.4)
- [ ] `/audit-reports` — Audit reports (separate from audit logs)

### 5.2 Page Enhancements
- [ ] `/purchases` — Add receiving workflow UI
- [ ] `/inventory` — Add stock transfer create/receive UI
- [ ] `/inventory` — Add inventory counts UI (or separate page)
- [ ] `/expenses` — Add edit/update functionality
- [ ] `/commissions` — Add create/delete functionality
- [ ] `/returns` — Add approval workflow
- [ ] `/receivables` — Add aging report view
- [ ] `/payables` — Add aging report view
- [ ] `/reports` — Add all missing report types + export
- [ ] `/products` — Add bulk import
- [ ] `/sales` — Add payment status update
- [ ] `/settings` — Add password change section
- [ ] `/vouchers` — Add edit/validate functionality

---

## Schema Changes Required

| Table | Change | Priority | Status |
|---|---|---|---|
| `purchaseItems` | Add `receivedQuantity` column | Phase 1 | Already exists |
| `journalEntries` | Add `referenceType`, `referenceId`, `isAutoGenerated`, `reversalEntryId` | Phase 1 | Already exists |
| `stockTransfers` | Add `transferNumber`, `userId`, `serialNumbers`, `receivedBy` | Phase 1 | Already exists |
| `cashRegisterSessions` | Add `reconciledBy`, `reconciledAt` | Phase 2 | Pending |
| `salePayments` | Add `notes` column | Phase 3 | Already exists |

---

## Not Applicable for Web (Desktop-Only Features)

These desktop features are intentionally excluded from the web app:

- [x] ~~Database Viewer~~ — Debug tool, not needed in production web app
- [x] ~~License Activation/Management~~ — Replaced by subscription system
- [x] ~~Database Reset/Export~~ — Managed at infrastructure level (Neon)
- [x] ~~Backup/Restore~~ — Handled by Neon PostgreSQL automated backups
- [x] ~~App Lock State~~ — Desktop-specific security
- [x] ~~Setup Wizard~~ — Replaced by signup flow + onboarding
- [x] ~~Manual Migration Handlers~~ — Handled by Drizzle migrations
- [x] ~~Machine ID / License Request~~ — SaaS subscription model

---

## Progress Tracker

| Phase | Total Items | Completed | % |
|---|---|---|---|
| Phase 1: Critical | 79 | 67 | 85% |
| Phase 2: Important | 69 | 59 | 86% |
| Phase 3: Moderate | 38 | 0 | 0% |
| Phase 4: Security | 14 | 0 | 0% |
| Phase 5: UI Pages | 18 | 0 | 0% |
| **Total** | **218** | **126** | **58%** |

---

*Generated: 2026-02-09 | Branch: `web_application`*
